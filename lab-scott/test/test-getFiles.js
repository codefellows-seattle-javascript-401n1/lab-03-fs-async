'use strict';

const assert = require('assert');
const getFiles = require(__dirname + '../lib/getFiles');

describe('Testing module lib/getFiles.js', function() {
  describe('Testing function readFile()', function() {
    it('Should pass; no test', function() {

    });
  });
});
